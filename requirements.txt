# PDF & Image processing
PyMuPDF>=1.24.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# PyTorch (CUDA build must be installed separately via the official index URL)
# For CUDA 12.4:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# For CUDA 12.1:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only:   pip install torch torchvision torchaudio
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Hugging Face & IndicOCR model stack
huggingface-hub>=0.24.0
transformers>=4.40.0
accelerate>=0.28.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
timm>=0.9.0
onnxruntime>=1.17.0

# Flash Attention 2 (optional but strongly recommended for Ampere+ GPUs — RTX 30xx/40xx, A100)
# Provides ~2x attention speedup and significantly lower VRAM usage.
# Pre-built wheels: https://github.com/Dao-AILab/flash-attention/releases
# Install with:  pip install flash-attn --no-build-isolation
flash-attn>=2.5.0

# LLM Client & Diffs (Phase 3)
httpx>=0.27.0
openai>=1.25.0
diff-match-patch>=20230430

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
pydantic>=2.6.0
